# Install needed stuff for each deployment instance
  - name: Install mandatory stuff
    hosts: '{{ hosts | default("localhost") }}'
    user: root

    roles:
      - _mandatory
    tags:
      - mandatory

# Install JAVA
  - name: Install java
    hosts: '{{ hosts | default("localhost") }}'
    user: root

    roles:
      - _java
    tags:
      - java